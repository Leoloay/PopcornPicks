<!DOCTYPE html>
<html lang="en">
  <style>
    body {
      margin: 0;
      font-family: "Fira Sans", sans-serif;
      background: linear-gradient(120deg, #0b0b0b, #1f1f1f);
      color: #ffffff;
      text-align: center;
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    h1 {
      font-family: "Yuji Mai", serif;
      color: #ffd700;
      font-size: 2.5rem;
      margin-bottom: 20px;
    }
  </style>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>HomePage</title>
    <%- include("partials/_navbar.ejs") %>
  </head>
  <body>
    <% if(user) { %>
    <h1>Welcome to PopcornPicks, <%= user.username %></h1>

    <% }else { %>
    <h1>Welcome to PopcornPicks, guest</h1>

    <% } %>
  </body>
</html>
