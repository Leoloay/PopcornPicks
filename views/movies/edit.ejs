<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Page</title>
  </head>
  <body>
    <form action="/movies/requests/<%=movies._id%>?_method=PUT" method="post">
      <label for="title">Title:</label>
      <input type="text" name="title" id="title" value="<%= movies.title %>" />
      <label for="genre">Genre:</label>
      <input type="text" name="genre" id="genre" value="<%= movies.genre %>" />
      <label for="length">Length:</label>
      <input
        type="text"
        name="length"
        id="length"
        value="<%= movies.length %>"
      />
      <label for="year">Year:</label>
      <input type="number" name="year" id="year" value="<%= movies.year %>" />
      <label for="plot">Plot:</label>
      <input type="text" name="plot" id="plot" value="<%= movies.plot %>" />
      <label for="approved">Approved?</label>
      <input type="checkbox" name="approved" id="approved" <%=
      movies.approved?'checked' : '' %> />
      <button type="submit">Submit your Request</button>
    </form>
  </body>
</html>
